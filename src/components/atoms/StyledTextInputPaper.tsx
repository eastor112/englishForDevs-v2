import styled from 'styled-components/native';
import {TextInput} from 'react-native-paper';

const StyledTextInputPaper = styled(TextInput)`
  margin-bottom: 20px;
`;

export default StyledTextInputPaper;
