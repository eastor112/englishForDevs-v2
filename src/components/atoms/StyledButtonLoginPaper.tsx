import {Button} from 'react-native-paper';
import styled from 'styled-components/native';

const StyledButtonLoginPaper = styled(Button)`
  margin-top: 15px;
  margin-bottom: 5px;
  padding: 5px 0px;
  font-size: 25px;
`;

export default StyledButtonLoginPaper;
